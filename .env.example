# ADMIN_USER and ADMIN_PASSWORD gate the /admin dashboard (HTTP Basic Auth,
# see src/middleware.ts). Without ADMIN_PASSWORD set, /admin 404s for
# everyone. Set these in Vercel's project env vars for production.
ADMIN_USER=admin
ADMIN_PASSWORD=
